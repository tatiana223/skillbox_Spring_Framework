app:
    init-sample-data: true

spring:
    shell:
        history:
            enable: true
        command:
            version:
                enabled: false

logging:
    level:
        com.example.skillboxsecondtask: INFO

